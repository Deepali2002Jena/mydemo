pipeline {
    agent any

    stages {
        stage('print the linux command output') {
            steps {
                sh 'echo $BUILD_ID'
				sh 'cal'
				sh 'date'
            }
        }
    }
}
